# UI 精修指令
> Medical AI Workspace · 2026/3/14
> 优先级从高到低，比赛前至少完成 P0

---

## P0 · 必须修，影响演示

### 1. 修复报告预览区的 Markdown 渲染

**问题**：`###` 符号直接显示在报告预览里，没有被渲染成标题。

**指令**：
```
报告预览区的内容字段，在渲染前用 marked.js 或 react-markdown 
处理原始文本，将 ### 转换为对应的 h3 标签。

如果来不及接入渲染库，临时方案：
将报告预览区的内容用 .replace(/###\s/g, '') 去掉 ### 符号，
至少不要让原始语法暴露在界面上。
```

---

### 2. 修复"成年人/职业"按钮颜色

**问题**：激活态用了紫色填充（`#7C3AED` 或类似值），和整体绿色主色调完全冲突，没有设计意图。

**指令**：
```
将训练模式按钮组的选中态颜色统一为主色调绿色。

激活态：bg-[#1B8A6B] 或你的主色绿，text-white
未激活态：bg-white，border-1，border-[#E2E8F0]，text-[#4A6B8A]

移除所有紫色相关的按钮样式。
```

---

## P1 · 强烈建议修，影响专业感

### 3. 删除所有页面说明文字

**问题**：每个页面顶部都有一段"本页用于……"的描述，界面在解释自己，说明视觉引导不够强。

**需要删除的文字**：
- 监控中心："本页用于完成本次训练监控评估与计划生成，完成后再进入报告中心。"
- 教练报告中心："聚合运动员体态、ROM、主观疲劳与动作稳定性数据，生成专业级 AI 训练调整建议。"
- 患者卡片内部："当前围绕本次监控完成体态监控、ROM 评估、疲劳问询及训练计划生成。页面只负责完成分析与计划任务，报告查看与导出统一在报告中心完成。"

**指令**：
```
直接删除以上三段 <p> 或 description 文字节点。
不要替换，就是删掉。
删完后检查布局是否塌陷，如果有，调整上下 padding 补回来。
```

---

### 4. 统一第二张页面四个筛选卡的交互形态

**问题**：同一行出现了下拉框、按钮组、进度条三种交互形态，视觉噪音大。

**指令**：
```
将"数据就绪状态"卡片重构：

删除进度条，改为以下布局：
- 卡片顶部：标题"数据就绪" + 右侧显示"2 / 4"大数字
- 卡片下半部：四个状态 tag 横排
  - 体态：绿色实心（已完成）
  - ROM：灰色空心（待完成）  
  - 语音：灰色空心（待完成）
  - 疲劳：绿色实心（已完成）

tag 样式：
已完成：bg-[#E1F5EE]，text-[#1B8A6B]，border-[#9FE1CB]
待完成：bg-[#F1F5F9]，text-[#94A3B8]，border-[#E2E8F0]
```

---

### 5. 压缩监控中心顶部空间

**问题**：面包屑 + 页面标题 + 说明文字 + badge 行，四层叠加，占了屏幕将近 1/3，内容还没开始。

**指令**：
```
顶部区域重构，合并为两行：

第一行：面包屑导航（保留，字号 12px，color: #94A3B8）
第二行：页面标题 "监控中心" + 右侧操作按钮组（三个按钮横排）

badge（待完成）和 监控ID 移到患者卡片内部，
紧跟在患者姓名右侧，不要单独占一行。

顶部区域总高度控制在 80px 以内。
```

---

### 6. 疲劳分析卡片行距

**问题**：第三张截图左侧"疲劳度与动作稳定性关联分析"卡片，文字行距太紧，读起来费力。

**指令**：
```
该卡片内 <p> 或正文文字节点：
line-height 从当前值改为 1.7
font-size 保持 13px 不变
padding 内边距四边统一 16px

卡片底部两个 tag（疲劳监测·已就绪 / 动作稳定性·已采集）
改为横排，间距 8px，不要竖排。
```

---

## P2 · 有时间再做

### 7. 患者卡片进度条位置

**问题**：进度信息（1/4）在右侧独立卡片里，和左侧患者信息视觉割裂。

**指令**：
```
将进度条从右侧独立卡片移入患者主卡片底部。

布局：
患者信息（姓名 + ID + 监控场次 + 更新时间）
---分割线---
评估进度  ████░░░░  1 / 4   继续完成监控 →

右侧独立的"本次监控进度"卡片删除。
```

---

### 8. 报告预览区左侧 AI 综合结论卡片

**问题**：文字密度高，缺乏视觉层次。

**指令**：
```
"疲劳度与动作稳定性关联分析"作为卡片标题，保持 bold 14px。

正文拆成两部分：
1. 核心结论（1-2句）：font-size 13px，color #1C2B3A
2. 建议（1-2句）：font-size 12px，color #4A6B8A，
   加左边距 12px 或左侧细线（border-left: 2px solid #E2E8F0）

底部两个 tag 改为横排。
```

---

## 执行顺序

```
Day 1（今天）：P0 全部完成 → 截图备用
Day 1（今天）：P1 中的 3、4 完成
Day 2（明天）：P1 中的 5、6 完成
比赛后：P2
```

---

## 截图建议

修完后，按这个顺序截三张用于PPT：

1. **监控中心** — 完整页面，展示患者列表和进度
2. **教练报告中心** — 筛选区域 + 数据就绪状态
3. **报告预览** — 右侧综合报告预览（确保 Markdown 已渲染）
