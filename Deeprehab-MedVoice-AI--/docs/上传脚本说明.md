# GitHub 上传自动化脚本

此脚本将帮助您自动将项目上传到 GitHub。

## 📋 使用前准备

### 1. 获取 GitHub 用户名

访问 https://github.com/settings 查看您的 GitHub 用户名。

**注意**：GitHub 用户名通常不是邮箱地址。

### 2. 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 填写信息：
   - Repository name: `aisci`
   - Description: `智能语音转病历助手 - 提高医疗工作效率的AI工具`
   - Public/Private: Public
   - Initialize with README: 不勾选
   - Add .gitignore: 不勾选
   - Choose a license: MIT License
3. 点击 "Create repository"

### 3. 配置 GitHub 认证

#### 方式一：使用 Personal Access Token（推荐）

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 填写信息：
   - Note: `AIsci Upload`
   - Expiration: 选择过期时间（推荐 90 days）
   - 勾选权限：`repo`（完整控制仓库）
4. 点击 "Generate token"
5. **重要**：复制生成的 token（只显示一次）

#### 方式二：使用 GitHub CLI（如果已安装）

```powershell
gh auth login
```

---

## 🚀 使用方法

### 方式一：手动执行命令

打开 PowerShell，执行以下命令：

```powershell
# 1. 进入项目目录
cd "c:\Users\23849\Desktop\插件\MCP DEV\AIsci"

# 2. 配置 Git 用户信息（替换 YOUR_USERNAME）
git config --global user.name "YOUR_USERNAME"
git config --global user.email "2384928576@qq.com"

# 3. 初始化 Git 仓库
git init

# 4. 添加所有文件
git add .

# 5. 提交更改
git commit -m "Initial commit: 语音转病例助手 v1.0.0"

# 6. 添加远程仓库（替换 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/aisci.git

# 7. 推送到 GitHub
git branch -M main
git push -u origin main
```

### 方式二：使用自动化脚本

1. 将下面的脚本保存为 `upload_to_github.ps1`
2. 在 PowerShell 中运行：
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   .\upload_to_github.ps1
   ```

---

## 📝 自动化脚本

```powershell
# upload_to_github.ps1
# GitHub 上传自动化脚本

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "  语音转病例助手 - GitHub 上传工具" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# 检查是否在正确的目录
$currentDir = Get-Location
$expectedDir = "c:\Users\23849\Desktop\插件\MCP DEV\AIsci"

if ($currentDir.Path -ne $expectedDir) {
    Write-Host "当前目录: $currentDir" -ForegroundColor Yellow
    Write-Host "预期目录: $expectedDir" -ForegroundColor Yellow
    Write-Host ""
    $response = Read-Host "是否切换到预期目录？(Y/N)"
    if ($response -eq "Y" -or $response -eq "y") {
        Set-Location $expectedDir
        Write-Host "已切换到: $expectedDir" -ForegroundColor Green
    } else {
        Write-Host "请手动切换到正确的目录后重新运行脚本" -ForegroundColor Red
        exit
    }
}

Write-Host ""

# 输入 GitHub 用户名
Write-Host "请输入您的 GitHub 用户名（不是邮箱地址）" -ForegroundColor Yellow
$username = Read-Host "GitHub 用户名"

if ([string]::IsNullOrWhiteSpace($username)) {
    Write-Host "错误: 用户名不能为空" -ForegroundColor Red
    exit
}

Write-Host ""

# 确认信息
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "上传信息确认" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "GitHub 用户名: $username" -ForegroundColor Green
Write-Host "邮箱: 2384928576@qq.com" -ForegroundColor Green
Write-Host "仓库名: aisci" -ForegroundColor Green
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

$response = Read-Host "确认以上信息正确？(Y/N)"
if ($response -ne "Y" -and $response -ne "y") {
    Write-Host "已取消上传" -ForegroundColor Yellow
    exit
}

Write-Host ""
Write-Host "开始上传..." -ForegroundColor Green
Write-Host ""

# 步骤 1: 配置 Git 用户信息
Write-Host "[1/7] 配置 Git 用户信息..." -ForegroundColor Cyan
git config --global user.name $username
git config --global user.email "2384928576@qq.com"
Write-Host "✓ Git 用户信息配置完成" -ForegroundColor Green
Write-Host ""

# 步骤 2: 初始化 Git 仓库
Write-Host "[2/7] 初始化 Git 仓库..." -ForegroundColor Cyan
git init
Write-Host "✓ Git 仓库初始化完成" -ForegroundColor Green
Write-Host ""

# 步骤 3: 添加所有文件
Write-Host "[3/7] 添加文件到 Git..." -ForegroundColor Cyan
git add .
Write-Host "✓ 文件添加完成" -ForegroundColor Green
Write-Host ""

# 步骤 4: 提交更改
Write-Host "[4/7] 提交更改..." -ForegroundColor Cyan
git commit -m "Initial commit: 语音转病例助手 v1.0.0"
Write-Host "✓ 提交完成" -ForegroundColor Green
Write-Host ""

# 步骤 5: 添加远程仓库
Write-Host "[5/7] 添加远程仓库..." -ForegroundColor Cyan
$remoteUrl = "https://github.com/$username/aisci.git"
git remote add origin $remoteUrl
Write-Host "✓ 远程仓库添加完成: $remoteUrl" -ForegroundColor Green
Write-Host ""

# 步骤 6: 重命名分支
Write-Host "[6/7] 重命名分支为 main..." -ForegroundColor Cyan
git branch -M main
Write-Host "✓ 分支重命名完成" -ForegroundColor Green
Write-Host ""

# 步骤 7: 推送到 GitHub
Write-Host "[7/7] 推送到 GitHub..." -ForegroundColor Cyan
Write-Host "这可能需要几分钟，请耐心等待..." -ForegroundColor Yellow
Write-Host ""

try {
    git push -u origin main
    Write-Host ""
    Write-Host "========================================" -ForegroundColor Green
    Write-Host "  上传成功！" -ForegroundColor Green
    Write-Host "========================================" -ForegroundColor Green
    Write-Host ""
    Write-Host "仓库地址: https://github.com/$username/aisci" -ForegroundColor Cyan
    Write-Host ""
    Write-Host "下一步:" -ForegroundColor Yellow
    Write-Host "1. 访问您的仓库查看文件" -ForegroundColor White
    Write-Host "2. 创建 Release（可选）" -ForegroundColor White
    Write-Host "3. 设置仓库信息（Topics、描述等）" -ForegroundColor White
    Write-Host "4. 分享项目链接" -ForegroundColor White
    Write-Host ""
    Write-Host "让 AI 助力医疗，让工作更高效！❤️" -ForegroundColor Green
} catch {
    Write-Host ""
    Write-Host "========================================" -ForegroundColor Red
    Write-Host "  上传失败！" -ForegroundColor Red
    Write-Host "========================================" -ForegroundColor Red
    Write-Host ""
    Write-Host "错误信息: $($_.Exception.Message)" -ForegroundColor Yellow
    Write-Host ""
    Write-Host "可能的原因:" -ForegroundColor Yellow
    Write-Host "1. GitHub 用户名不正确" -ForegroundColor White
    Write-Host "2. 仓库尚未在 GitHub 上创建" -ForegroundColor White
    Write-Host "3. 需要认证（使用 Personal Access Token）" -ForegroundColor White
    Write-Host ""
    Write-Host "解决方案:" -ForegroundColor Yellow
    Write-Host "1. 访问 https://github.com/settings 检查用户名" -ForegroundColor White
    Write-Host "2. 访问 https://github.com/new 创建仓库" -ForegroundColor White
    Write-Host "3. 访问 https://github.com/settings/tokens 创建 Token" -ForegroundColor White
    Write-Host "4. 使用 Token 推送: git push -u origin main" -ForegroundColor White
    Write-Host ""
}
```

---

## 🔧 故障排除

### 问题 1: 认证失败

**错误信息**：
```
remote: Invalid username or password.
fatal: Authentication failed
```

**解决方案**：

1. 创建 Personal Access Token：
   - 访问 https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 选择 `repo` 权限
   - 生成并复制 token

2. 使用 Token 推送：
   ```powershell
   git remote set-url origin https://YOUR_TOKEN@github.com/YOUR_USERNAME/aisci.git
   git push -u origin main
   ```

### 问题 2: 仓库不存在

**错误信息**：
```
remote: Repository not found.
fatal: repository 'https://github.com/xxx/aisci.git' not found
```

**解决方案**：

1. 访问 https://github.com/new
2. 创建名为 `aisci` 的仓库
3. 重新执行推送命令

### 问题 3: 用户名不正确

**解决方案**：

1. 访问 https://github.com/settings
2. 查看页面顶部的用户名
3. 使用正确的用户名重新配置：
   ```powershell
   git config --global user.name "CORRECT_USERNAME"
   git remote set-url origin https://github.com/CORRECT_USERNAME/aisci.git
   git push -u origin main
   ```

---

## ✅ 上传后检查清单

- [ ] 仓库已成功创建
- [ ] 所有文件已上传
- [ ] README.md 显示正常
- [ ] LICENSE 文件存在
- [ ] 代码文件完整

---

## 🎯 下一步

上传成功后：

1. **创建 Release**：
   ```powershell
   git tag -a v1.0.0 -m "Release v1.0.0"
   git push origin v1.0.0
   ```

2. **设置仓库信息**：
   - 添加 Topics 标签
   - 填写仓库描述
   - 设置仓库网站

3. **分享项目**：
   - 分享仓库链接
   - 发布到社交媒体
   - 提交到开源目录

---

## 📞 需要帮助？

- [GitHub 文档](https://docs.github.com/)
- [GitHub 社区](https://github.community/)
- [上传命令.md](上传命令.md)
- [GitHub上传清单.md](GitHub上传清单.md)

---

**让 AI 助力医疗，让工作更高效！** ❤️
