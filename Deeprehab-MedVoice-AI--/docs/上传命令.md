# GitHub 上传命令

## 📋 准备工作

### 1. 确认 GitHub 用户名

请访问 https://github.com/settings 查看您的 GitHub 用户名。

**注意**：GitHub 用户名通常不是邮箱地址，而是您注册时设置的用户名。

### 2. 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - **Repository name**: `aisci`
   - **Description**: `智能语音转病历助手 - 提高医疗工作效率的AI工具`
   - **Public/Private**: 选择 Public（推荐）
   - **Initialize with README**: 不勾选（我们已经有 README.md）
   - **Add .gitignore**: 不勾选（我们已经有 .gitignore）
   - **Choose a license**: 选择 MIT License
3. 点击 "Create repository"

---

## 🚀 上传步骤

### 步骤 1：打开 PowerShell

在项目目录下打开 PowerShell：

```powershell
cd "c:\Users\23849\Desktop\插件\MCP DEV\AIsci"
```

### 步骤 2：配置 Git 用户信息

```powershell
git config --global user.name "your-username"
git config --global user.email "2384928576@qq.com"
```

**重要**：将 `your-username` 替换为您的 GitHub 用户名（不是邮箱）。

### 步骤 3：初始化 Git 仓库

```powershell
git init
```

### 步骤 4：添加所有文件

```powershell
git add .
```

### 步骤 5：提交更改

```powershell
git commit -m "Initial commit: 语音转病例助手 v1.0.0"
```

### 步骤 6：添加远程仓库

```powershell
git remote add origin https://github.com/your-username/aisci.git
```

**重要**：将 `your-username` 替换为您的 GitHub 用户名。

### 步骤 7：推送到 GitHub

```powershell
git branch -M main
git push -u origin main
```

---

## 📝 完整命令（复制粘贴版）

请按顺序执行以下命令：

```powershell
# 1. 进入项目目录
cd "c:\Users\23849\Desktop\插件\MCP DEV\AIsci"

# 2. 配置 Git 用户信息（替换 your-username）
git config --global user.name "your-username"
git config --global user.email "2384928576@qq.com"

# 3. 初始化 Git 仓库
git init

# 4. 添加所有文件
git add .

# 5. 提交更改
git commit -m "Initial commit: 语音转病例助手 v1.0.0"

# 6. 添加远程仓库（替换 your-username）
git remote add origin https://github.com/your-username/aisci.git

# 7. 推送到 GitHub
git branch -M main
git push -u origin main
```

---

## ⚠️ 重要提示

### 替换用户名

在上面的命令中，需要将两处的 `your-username` 替换为您的实际 GitHub 用户名：

1. `git config --global user.name "your-username"`
2. `git remote add origin https://github.com/your-username/aisci.git`

### 如何找到 GitHub 用户名

1. 访问 https://github.com/settings
2. 在页面顶部可以看到您的用户名
3. 或者访问 https://github.com/your-username（将 your-username 替换为可能的用户名）

### 如果推送失败

如果遇到认证错误，可能需要：

1. **使用 Personal Access Token**：
   - 访问 https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 选择权限：`repo`（完整控制仓库）
   - 生成 token 并复制

2. **使用 Token 推送**：
   ```powershell
   git remote set-url origin https://your-token@github.com/your-username/aisci.git
   git push -u origin main
   ```

---

## ✅ 验证上传

上传完成后，访问以下链接验证：

```
https://github.com/your-username/aisci
```

您应该能看到：
- README.md 文件
- 所有代码文件
- 所有文档文件

---

## 🎯 下一步

上传成功后，您可以：

1. **创建 Release**：
   ```powershell
   git tag -a v1.0.0 -m "Release v1.0.0"
   git push origin v1.0.0
   ```
   然后在 GitHub 上创建 Release。

2. **设置仓库信息**：
   - 添加 Topics 标签
   - 填写仓库描述
   - 设置仓库网站

3. **分享项目**：
   - 分享仓库链接
   - 发布到社交媒体
   - 提交到开源目录

---

## 📞 需要帮助？

如果遇到问题：

1. 查看 [GitHub 文档](https://docs.github.com/)
2. 搜索 [GitHub 社区](https://github.community/)
3. 检查错误信息并搜索解决方案

---

## 🎉 完成

上传成功后，您的项目将公开在 GitHub 上！

**仓库地址**：https://github.com/your-username/aisci

**让 AI 助力医疗，让工作更高效！** ❤️


git@github.com:Drehabwen/Rehab.git

