# 后端体态评估规则和数据

## 1. 核心计算方法

### 1.1 坐标中心化
- **方法**：`center_coordinates`
- **功能**：将所有骨骼点坐标相对于右侧髋关节（默认）进行中心化
- **目的**：消除身体位置对评估结果的影响，使评估更加准确

### 1.2 统计计算
- **方法**：`calculate_statistics`
- **功能**：计算每个骨骼点的平均值、标准差和趋势斜率
- **输出**：包含每个骨骼点统计数据的字典

### 1.3 具体指标计算

#### 1.3.1 高低肩角度
- **方法**：`calculate_shoulder_angle`
- **计算方式**：基于左右肩膀Y坐标差异，使用反正切函数计算角度
- **方向**：正值表示右肩高于左肩，负值表示左肩高于右肩
- **阈值**：> 2° 视为异常

#### 1.3.2 头部旋转角度
- **方法**：`calculate_head_rotation`
- **计算方式**：基于鼻子X坐标偏差，使用反正切函数计算角度
- **阈值**：> 5° 视为异常

#### 1.3.3 头部侧倾角度
- **方法**：`calculate_head_tilt`
- **计算方式**：基于鼻子X坐标偏差的绝对值，使用反正切函数计算角度
- **阈值**：> 5° 视为异常

#### 1.3.4 头前伸距离
- **方法**：`calculate_head_forward`
- **计算方式**：基于鼻子Z坐标与肩膀Z坐标平均值的差值
- **单位**：厘米
- **阈值**：> 3cm 视为异常

#### 1.3.5 骨盆倾斜角度
- **方法**：`calculate_pelvic_tilt`
- **计算方式**：基于左右髋关节Y坐标差异，使用反正切函数计算角度
- **方向**：正值表示右髋高于左髋，负值表示左髋高于右髋
- **阈值**：> 2° 视为异常

#### 1.3.6 稳定性评分
- **方法**：`calculate_stability_score`
- **计算方式**：基于所有骨骼点的标准差，转换为0-100的评分
- **评分标准**：
  - >= 80：优秀
  - >= 60：良好
  - >= 40：一般
  - < 40：较差

## 2. 评估规则和阈值

### 2.1 `narrator.py` 中的评估规则

| 指标 | 正常范围 | 异常标准 | 描述 |
|------|---------|----------|------|
| 高低肩 | <= 2° | > 2° | 检测到高低肩现象，显示方向和角度 |
| 头部旋转 | <= 5° | > 5° | 检测到头部旋转，显示角度 |
| 头部侧倾 | <= 5° | > 5° | 检测到头部侧倾，显示角度 |
| 头前伸 | <= 3cm | > 3cm | 检测到头前伸，显示距离 |
| 骨盆倾斜 | <= 2° | > 2° | 检测到骨盆倾斜，显示方向和角度 |
| 稳定性 | >= 80 | < 80 | 评估整体稳定性，显示评分和等级 |

### 2.2 `config.py` 中的姿态分析阈值

| 指标 | 轻度异常 | 中度异常 | 重度异常 |
|------|---------|---------|----------|
| 头前倾 | - | 0.25 | 0.45 |
| 圆肩/含胸 | 0.15 | - | - |
| 头部侧倾 | 0.03 | 0.08 | - |
| 高低肩 | 0.03 | 0.08 | - |
| 骨盆侧倾 | 0.03 | 0.08 | - |
| 身体中线偏移 | - | 0.08 | - |

## 3. 报告生成流程

1. **坐标中心化**：将骨骼点坐标相对于右侧髋关节进行中心化
2. **统计计算**：计算每个骨骼点的平均值、标准差和趋势斜率
3. **指标计算**：计算各个体态指标的具体数值
4. **生成报告**：根据指标数值和阈值，生成结构化的评估报告

## 4. 输出数据结构

```python
{
    "view": "front",  # 评估视角
    "stats": {  # 骨骼点统计数据
        "nose": {
            "mean": {"x": 0.1, "y": -0.5, "z": 0.2},
            "std": {"x": 0.01, "y": 0.01, "z": 0.01},
            "trend": {"x": 0.0, "y": 0.0, "z": 0.0}
        },
        # 其他骨骼点...
    },
    "metrics": {  # 计算的体态指标
        "shoulderAngle": 1.5,  # 高低肩角度
        "headRotation": 3.2,   # 头部旋转角度
        "headTilt": 2.1,       # 头部侧倾角度
        "headForward": 1.8,    # 头前伸距离（cm）
        "pelvicTilt": -0.8,    # 骨盆倾斜角度
        "stabilityScore": 85.5 # 稳定性评分
    },
    "narration": "...",  # 结构化的叙述报告
    "legacy_narration": "..."  # 旧格式报告（向后兼容）
}
```

## 5. 关键评估逻辑

1. **高低肩分析**：当肩膀角度绝对值大于2°时，检测为高低肩
2. **头部姿势分析**：当头部旋转、侧倾大于5°或头前伸大于3cm时，检测为异常
3. **骨盆分析**：当骨盆倾斜角度绝对值大于2°时，检测为骨盆倾斜
4. **稳定性评估**：基于所有骨骼点的标准差计算稳定性评分，反映身体姿态的稳定程度

这些规则和数据构成了后端体态评估的核心逻辑，用于生成准确、客观的体态分析报告。